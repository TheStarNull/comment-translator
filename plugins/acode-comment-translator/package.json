{
  "name": "acode-comment-translator",
  "version": "2.6.0",
  "private": true,
  "description": "Acode plugin: translate JSDoc and code comments with the comment-translator engine.",
  "scripts": {
    "build": "node build.mjs",
    "build:release": "node build.mjs --production",
    "pack": "node pack.mjs",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node build-test.mjs && node dist/test.cjs"
  },
  "devDependencies": {
    "acode-plugin-types": "^1.12.9",
    "esbuild": "^0.25.0",
    "typescript": "^5.3.2"
  }
}
